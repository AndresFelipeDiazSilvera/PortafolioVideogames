---
import projects from "../collections/projects.json";
import PageHeading from "../components/page-heading.astro";
import Project from "../components/project.astro";
import Layout from "../layouts/main.astro";
---

<Layout title="Mis Proyectos">
  <section class="relative z-20 max-w-2xl mx-auto my-12 px-7 lg:px-0">
    <PageHeading
      title="Mis Proyectos y Prototipos"
      description="Aca encontraras una lista de mis proyectos y prototipos que he realizado a lo largo de mi carrera como desarrollador de videojuegos. Algunos son proyectos personales, otros son trabajos realizados en grupo en game jams, todos son proyectos que me han ayudado a aprender y mejorar mis habilidades como desarrollador de videojuegos."
    />

    <div
      class="z-50 grid items-stretch w-full grid-cols-1 my-8 gap-7 sm:gap-5 sm:grid-cols-2"
    >
      {
        projects.map((project) => {
          return (
            <Project
              name={project.name}
              description={project.description}
              image={project.image}
              url={project.url}
            />
          )
        })
      }
    </div>
  </section>
</Layout>



